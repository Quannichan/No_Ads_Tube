define([
        "handlebars"], 
    function(
    hbs
    )
    {
    // Handlebars.registerHelper('if', function(check) {
    //     if(check){
    //         return 
    //     }
    // });

    return hbsHelper
})